export default function ErrorNotFound() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'column',
        height: '800px',
        width: '100%',
        fontSize: '32px',
        fontWeight: '700',
      }}
    >
      404 - Page not Found
    </div>
  );
}
